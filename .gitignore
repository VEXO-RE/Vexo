# ── Secretos — NUNCA subir ──────────────────────────────────
.env
.env.local
.env.*.local
*.env

# ── Node / Build ─────────────────────────────────────────────
node_modules/
dist/
build/
.next/
out/
.vite/

# ── Logs, temporales y debug ──────────────────────────────────
*.log
*.swp
*.tmp
*.bak
*.cache
validation.log
stderr.txt
stdout.txt
filesystem_stderr.txt
filesystem_stdout.txt
fs_stderr.txt
fs_stdout.txt
node-local_stderr.txt
node-local_stdout.txt
python-local_stderr.txt
python-local_stdout.txt
powershell-local_stderr.txt
powershell-local_stdout.txt
scripts/*.log
scripts/*.txt
temp_*.ps1
temp_*.py

# ── Datos crudos (van en Drive, no en git) ───────────────────
*.csv
*.xlsx
*.xls
*.tsv

# ── Scripts sensibles con credenciales ───────────────────────
publicar_en_vercel.ps1
Sincronizar_Vexo.ps1
revisar_fugas.ps1

# ── Herramientas de terceros / AI ────────────────────────────
.refact/
.sixth/
.history/

# ── Vercel ───────────────────────────────────────────────────
.vercel

# ── Sistema operativo ─────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.lnk
ehthumbs.db

# ── VSCode: solo excluir lo que NO debe compartirse ──────────
# NOTA: settings.json, tasks.json y mcp.json SI se incluyen
.vscode/*.log

# ── Claude local (SESSION.json si no quieres compartirlo) ─────
# Descomenta si no quieres subir el estado de sesion:
# .claude/SESSION.json

# ── Archivos grandes (van en Drive) ──────────────────────────
public/downloads/
public/videos/
